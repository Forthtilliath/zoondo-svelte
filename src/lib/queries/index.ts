import * as actions from './actions';
import * as cards from './cards';
import * as games from './games';
import * as messages from './messages';
import * as users from './users';

const db = { actions, cards, users, messages, games };

export default db;